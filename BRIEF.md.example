# Brief

Read these source files:

- /absolute/path/to/source-1.md
- /absolute/path/to/source-2.md

Produce:

- A v0.1 plan spec in `SPEC.md`.

Additional guidance:

- This is a CLI tool, so consider the requirements, standards, and goals in
  the make-cli skill.
- Treat the source files as context unless this brief or `CONSTRAINTS.md`
  says a point is mandatory.

Hard constraints:

- Add only constraints that are truly binding. If the list gets long, put them
  in `CONSTRAINTS.md` instead.
